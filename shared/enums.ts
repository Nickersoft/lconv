export enum LanguageStatus {
  Active = "A",
  Deprecated = "R",
}

export enum LanguageScope {
  Individual = "I",
  Macro = "M",
  Special = "S",
}

export enum LanguageType {
  Living = "L",
  Constructed = "C",
  Extinct = "E",
  Ancient = "A",
  Historical = "H",
  Special = "S",
}
